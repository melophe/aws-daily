TLS は通信を守るけど、各中間層（CloudFront・ALB）で一度復号されて平文になる。
Field-Level Encryption（FLE）は、その平文露出すら防ぐ。
データの特定フィールドは最後のECSマイクロサービスでしか復号できない。
ALB（Application Load Balancer）では、FLEの復号はできません。
FLEの仕組み上、「CloudFrontが暗号化 → アプリ（ECSなど）が復号」という設計になっていて、
ALBは単なる“暗号文の通過点”

DNS名は「IPを抽象化」して運用を楽にするためにある