# AWS 学習メモ

## SFTP とデータ転送
- SFTP = SSH を使った安全なファイル転送プロトコル
- DataSync = NFS/SMB/HDFS/S3/EFS/FSx 専用（SFTPは対象外）
- SFTPを扱うなら AWS Transfer Family を使うのがベスト

---

## データベース移行

- AWS DMS = データベース移行サービス
- スキーマ変換には AWS SCT を併用

---

## CloudFormation

### 変更管理
変更セットは事前に確認

### 参照方法
- 同じスタック内: Ref, Fn::GetAtt
- 別スタック間: Export / ImportValue

---

## AWS Organizations

AWS Organizations を作成した直後は、「FullAWSAccess」 という SCP が全 OU とアカウントにアタッチ

- FullAWSAccess = すべてのサービス／アクションを許可
- SCP は「この範囲までしか許可できない」という上限

---

## ネットワーク・セキュリティ

### ポート番号
1433番: Microsoft SQL Server (MSSQL) がデフォルトで使用する TCP ポート番号

### エフェメラルポート
クライアントがサーバーに通信するときに 一時的に自動割り当てされるポート番号

---

## CloudFront

CloudFrontには価格クラスがあり高いほど速い

---

## AWS ParallelCluster

スーパーコンピュータ環境をAWS上で作成するサービス

---

## DynamoDB トランザクション

### 基本機能
- 複数操作をまとめて1つのトランザクションに
- 最大 25 個の操作をまとめられる
- 複数テーブルにまたがってもOK
- ACID 特性

### 実用例
DynamoDBテーブルに対して複数の項目の追加、更新、または削除を一つのオペレーションとして管理可能

例：
- 「新しいアイテムを追加する (Put)」
- 「もし機密フラグが立っていたら削除する (ConditionCheck + Delete)」

これらを1つのトランザクションにまとめることで、中途半端な状態が残らない

---

## SQS 優先度制御

複数キューを作って、それを優先度付きのように扱う

### 構成例
- 有料ユーザー用 → HighPriorityQueue
- 無料ユーザー用 → StandardQueue

### 処理ロジック
1. HighPriorityQueue を優先的にポーリング
2. 空なら StandardQueue を処理

---

## S3データ転送の高速化

Amazon S3 Transfer Acceleration

---

## ACM 証明書

ACM を利用する場合、EC2にインストールするのではなくELBにインストール

---

## NACL の挙動

### デフォルトNACL
- Inbound / Outbound ともに すべて許可 (Allow All)
- だから特に何もしなくても通信可能

### カスタムNACL
- Inbound / Outbound のルールを自分で書く必要がある
- ステートレスなので 行きと戻りを両方許可しないと通信不可
- つまり「Inboundだけ許可してもNG」

### SSHの場合
- Inbound: ポート22を許可
- Outbound: クライアントのエフェメラルポート(1024–65535)を許可

両方そろって初めてSSH接続が可能

ステートレス！

---

## Redshift

### WorkLoad Management
Redshift でクエリを効率的に処理するクエリキュー管理システム

クエリを「キュー」に入れて、リソース（メモリ・並列実行枠）を配分

---

## その他

### DMS vs SCT
- **DMS** = データ移行
- **SCT** = スキーマ変換

### PrivateLink 接続条件
「サードパーティのアプリが AWS 上にある」＋「プライベートに接続したい」＋「インターネット接続は最小限」という条件

---

## IOPS について

### 定義
- IOPS = Input/Output Operations Per Second
- 1秒あたりの入出力処理回数

### 補足
- 「IO」= 入出力を意味する
- 「io1 / io2」= 高IOPSを提供するSSD

---

## SQS アクセスポリシー

SQS → アクセスポリシー必要（どのサービスからメッセージ受け取れるか明示）

---

## CloudFormationのEgressの意味

- **Egress: true** → アウトバウンドルール（送信）
- **Egress: false** → インバウンドルール（受信）

---

## Amazon IVS vs AWS Elemental

### Amazon IVS
- ライブ配信に特化、超低遅延
- 開発者向けに「配信機能をアプリに組み込む」用途

### AWS Elemental MediaLive / MediaPackage
- 本格的な映像配信プラットフォーム構築向け

---

## Container Insights と X-Ray の併用

Container Insights で「Pod に余裕があるのに遅いな？」と気づき、X-Ray で「サービス間の呼び出しが遅い」と特定

### レイヤーの違い

#### CloudWatch Container Insights
- 見ているのは インフラ／コンテナリソースのレイヤー
- 例えば：
  - Pod がメモリ 95% 消費
  - Node の CPU が 80% で逼迫
  - ネットワーク I/O が増えてる
- 粗い粒度（インフラやコンテナ単位） での状況把握に強い

#### AWS X-Ray
- 見ているのは アプリケーションリクエストのレイヤー（分散トレーシング）
- 例えば：
  - API Gateway → Auth Service → Order Service → DB という呼び出し経路
  - Order Service 内の「SQL 実行」が 1.2 秒かかっている
  - 外部 API 呼び出しが失敗している
- より細かい粒度（リクエストごと・処理ステップごと） での可視化が可能

---

## AppFlow

サードパーティ SaaS（Salesforce, Slack, Zendesk, ServiceNow など）との連携 = AppFlow

---

## AWS Global Accelerator

AWS グローバルネットワークを使って、世界中から ALB/NLB/EC2 へのアクセスを最適化

### 特徴
- エニーキャスト IP による最短経路ルーティング & 可用性向上
- 直接構成できるのは ALB または NLB
- 推奨は NLBとの統合（レイヤー的に自然かつ高速）

---

## スケジュール方式比較

### cron方式
- 「スケジューラ専用の EC2 サーバーを常時稼働」が必要
- インスタンス代が発生し、管理も必要

### Lambda + EventBridge 方式
- 「AWSのマネージドなスケジューラ（EventBridge）が Lambda を呼び出す」
- サーバーレスなので余計なEC2が不要

---

## DynamoDB のキャパシティーモード

### プロビジョンド（固定キャパシティー）
- ベースラインは安いが、スパイクに弱い
- Auto Scaling を有効化すると多少は追従可能だが、スパイクに完全には対応しきれない

### オンデマンド（従量課金）
- リクエストに応じて自動スケーリング
- スパイクに即応（数百万 RCU/WCU まで対応可能）
- 「使った分だけ課金」なので、普段は低負荷・時々スパイクのケースでコスト効率が高い

### DynamoDB スケーリングの違い
- DynamoDBのAuto Scaling = プロビジョンドモード専用の機能、反応は数分かかるので急なスパイクには弱い
- オンデマンドモード = Auto Scaling 不要、完全自動

---

## FSBP (Foundational Security Best Practices)
- AWS が提供している セキュリティベストプラクティス基準
- AWS Security Hub で有効化できるセキュリティ標準の 1 つ
- AWS アカウントやリソースがベストプラクティスに従っているかどうかを自動チェック

### OLTP vs OLAP
- **OLTP (Online Transaction Processing)**: 日常業務向け（トランザクションを速く正確に処理）
- **OLAP (Online Analytical Processing)**: データ分析向け（大量データを集計・解析）

### Storage Gateway
- **キャッシュ型ボリュームゲートウェイ**: プライマリーはS3ストレージ
- **ボリュームゲートウェイ**: プライマリーはオンプレミスストレージ

### Apache Flink vs Firehose

リアルタイムでクエリ処理 = Amazon Managed Service for Apache Flink

- Flinkから直接S3に書き込める
- Firehoseは不要

#### 使い分け
- リアルタイム処理あり → Flink → S3 で十分
- 処理なしでストレージ保存だけ → Firehose を使う

---

### SCP vs IAM ポリシー

- SCP = 「何をやってもいいかの上限」
- IAMポリシー = 「実際にやっていい操作」

#### 重要な制約
- SCPだけでは実際の操作はできない
- 実際に操作するには IAMポリシーでも許可されている必要がある

---

## VPN 関連用語

### Customer Gateway (CGW)
オンプレ側のVPNルーターをAWSに登録した論理的リソース

### Virtual Private Gateway (VGW)
VPCにアタッチして、VPNのAWS側の入り口

### Transit Gateway (TGW)
複数VPCやオンプレをまとめて接続できるハブ

### 接続構成
CGW（オンプレ） ↔ VGW/TGW（AWS）でSite-to-Site VPNが張られる

---

## データ移行サービスの違い

### Storage Gateway
- 「オンプレとAWSストレージをつなぐブリッジ」
- データ移行の専用サービスではない
- 継続的に「オンプレとクラウドを共通ストレージのように扱う」ためのサービス

### DataSync
一度データをAWSに「移行」するためのサービス

### 使い分け
- **大量データを一度移行したい** → DataSync or Snowball
- **オンプレとS3をシームレスに使いたい** → Storage Gateway (File Gateway)

### 選択基準
- 「ネットで転送できるなら DataSync、ネットじゃ無理なら Snowball」
- 「100TB超えて帯域不足なら Snowball Edge、それ以上なら Snowmobile」

---

## Amazon FSx

### 概要
マネージドファイルシステムの総称

### サービス別用途
- **Windows環境** → FSx for Windows File Server
- **HPCやS3連携** → FSx for Lustre
- **エンタープライズNetApp** → FSx for ONTAP
- **Linux/ZFSワークロード** → FSx for OpenZFS

---

## RAID 構成比較

| 目的 | 推奨RAID | 理由 |
|------|----------|------|
| **可用性向上・RTO短縮** | RAID 1 | 冗長性を重視 |
| **性能向上** | RAID 0 | 読み書きを高速化（ストライピング） |

---

## ACM 証明書適用可能サービス

### 対応サービス一覧
- Elastic Load Balancing (ELB/ALB/NLB)
  - ALB（Application Load Balancer）
  - NLB（Network Load Balancer、TLSリスナー使用時）
  - CLB（Classic Load Balancer、HTTPSリスナー使用時）
- Amazon CloudFront
- Amazon API Gateway
- AWS App Runner
- AWS Elastic Beanstalk

---

## Aurora Global Database

### 特徴
- グローバルデータベース (Global Database) は Aurora 専用機能
- RDSではクロスリージョンリードレプリカのみ

### Aurora Global Database の利点
- Aurora の分散ストレージアーキテクチャを使用
- リージョン間を1秒未満の遅延で同期
- グローバル規模での高速レプリケーション＆フェイルオーバーが可能

---

## Route 53 ホストゾーン

### 概要
- ドメインの管理単位
- ドメイン名に対してDNSレコードをまとめて管理する箱

### パブリックホストゾーン
- ユーザー向けのWebサイトやサービス公開
- 例: www.example.com

### プライベートホストゾーン
- 社内だけがアクセスするアプリケーションやDB
- 例: db.internal

### Hosted Zone の構成

example.com の Hosted Zone 内のレコード例:

- **Aレコード**: www.example.com → 203.0.113.10
- **CNAMEレコード**: app.example.com → myalb-123456.elb.amazonaws.com
- **MXレコード**: example.com → mail.example.net
- **TXTレコード**: example.com → "v=spf1 include:amazonses.com"

### まとめ
Hosted Zone = ドメインの「DNSレコードをしまう箱」、レコードの種類ごとに役割が異なる

---

## Route 53 Traffic Flow

### 概要
GUIベースでポリシーを組み合わせられる強力な機能

### 特徴
- 複数のルーティングポリシーを**ツリー構造（ポリシーツリー）**として定義可能
- ユーザーの地域を判定 → 該当地域内でレイテンシーベースルーティング → 新旧システムに加重ルーティング
- 複雑な条件分岐を管理画面から可視化・運用できる

---

## Traffic Flow 制約

### 複数ポリシー組み合わせ制約
Traffic Flow を使わずに複数のルーティングポリシーを直接組み合わせることは不可能

### 使い分け
- **単一ポリシー**: Traffic Flow なしでも設定可能
- **複数ポリシーの階層的組み合わせ**: Traffic Flow 必須

---

## AD サービス比較

### AD Connector

#### 概要
- 「Active Directory を構築する」ものではない
- オンプレミスに既存のADが必須
- AWSにADを新しく立てるわけではない

#### 仕組み
- AWSリソース（WorkSpaces や Amazon RDS for SQL Server など）が認証を必要とする際に既存のオンプレADにプロキシ接続するだけ
- AWS上のディレクトリサービス → オンプレのActive Directoryへ認証リクエストをプロキシ
- 既存のオンプレADをそのまま利用するための中継役

### AWS Managed Microsoft AD

#### 概要
AWS上にマネージドなMicrosoft Active Directoryを構築するサービス

#### オンプレADとの関係
- **オンプレADは必須ではない**
- オンプレにADがなくてもAWS Managed Microsoft ADだけでディレクトリ基盤を構築可能
- オンプレADが存在する場合はトラスト関係を張って統合可能

#### 統合機能
- オンプレにADがあればAWS Managed Microsoft AD と信頼関係を構築
- オンプレADのユーザーがAWSのリソースへシングルサインオン可能

### まとめ
- **AD Connector** = 既存のオンプレADをそのまま利用するための中継役
- **AWS Managed Microsoft AD** = AWS上に新規にマネージドADを構築できるサービス

---

## EC2 vCPU制限

### 基本概念
- 手動やASGでインスタンス増やすときはそのインスタンスの vCPU × 台数で消費量を計算
- リージョンごとの上限を超えると起動できない
- インスタンス起動の制限は「インスタンス数」ではなく「vCPU数の合計」

### インスタンスタイプとvCPU
各EC2インスタンスには固定のvCPU数が割り当てられている

### デフォルト制限例
東京リージョンではデフォルトで「オンデマンドインスタンスのvCPUは64まで」という制限があるケースが多い

---

## データ検出サービス比較

- **Glue Sensitive Data Detection** = データカタログ／ETLの段階でPIIを検出
- **Amazon Macie** = セキュリティ監査・コンプライアンス用にS3全体をモニタリング

---

## Lake Formation データ共有

### 使い分け
- **シンプルに外部企業（特定アカウント）と共有** → Lake Formation クロスアカウント共有
- **ビジネス的にデータ配布（契約やサブスクリプション込み）** → AWS Data Exchange 連携

---

## Firehose ネイティブ変換機能

### Record Format Conversion サポート形式
Kinesis Data Firehose でサポート:
- JSON → Apache Parquet
- JSON → Apache ORC

### CSV変換について

#### 制限
Firehose単体ではCSVへの変換機能なし

#### 解決方法
AWS Lambda (Data Transformation) を挟んで、JSONをCSVに変換してからS3に出力

### 変換方法まとめ
- **Parquet / ORC** → Firehose単体で変換可能
- **CSV** → Firehose + Lambda（または後段でGlue/EMR処理）

### 推奨事項
AthenaやRedshift Spectrumで分析する目的なら CSVよりParquetやORCを選ぶ方が圧縮効率もクエリ性能も良い

### カスタム変換
Firehose単体でも「Parquet / ORC変換」はできるが、ニーズに応じた変換にはLambdaの連携が必要

---

## Kinesis Data Streams 制限

### S3保存について
Kinesis Data Streams 単体では S3 に直接保存不可

### 必要な組み合わせ
Firehose や Lambda を組み合わせる必要がある

---

## Splunk

### 概要
マシンデータ（ログ、メトリクス、イベントデータなど）を収集・検索・可視化・分析できるプラットフォーム

---

## AWS Transfer Family

### 概要
SFTP/FTPS/FTP 経由でファイルを転送できるマネージドサービス

### 機能
- 従来オンプレミスや企業間でよく使われているファイル転送プロトコルをそのままクラウド上に持ち込み
- Amazon S3 や Amazon EFS に直接保存可能

---

## DataSync エージェント要件

### オンプレからAWSへのデータ送信
DataSyncエージェントが必要

### 使い分け
- **オンプレからデータを送る** → DataSyncエージェントが必要
- **AWS内のサービス間コピー** → エージェント不要

---

## File Gateway

### 用途
オンプレから S3 をファイル共有（NAS風）で使用

### サポートプロトコル
NFS, SMB

### 特徴
- オンプレアプリから「普通のファイル共有」としてアクセス可能
- データは S3 にオブジェクトとして保存され、オンプレにはキャッシュが残る
- S3 のネイティブなオブジェクト形式（プレフィックスやライフサイクル管理）をそのまま利用可能

### ユースケース
- 研究データ、ログファイル、分析基盤への入力データ
- オンプレアプリがファイル共有しか使えない場合の S3 ブリッジ

---

## Volume Gateway

### 用途
オンプレのブロックストレージ（iSCSI）をクラウド連携

### プロトコル
iSCSI (ブロックデバイスとして提供)

### モード

#### Cached Volume モード
- 頻繁に使うデータはオンプレにキャッシュ
- マスターコピーはS3に保存

#### Stored Volume モード
- データの完全コピーはオンプレに保持
- 非同期でS3にバックアップ（EBSスナップショットとして保存）

### ユースケース
- オンプレのサーバーがブロックストレージ（ディスク）としてしか扱えない場合
- ディザスタリカバリ（オンプレ障害時にEBSスナップショットから復旧）

---

## プロトコル解説

### NFS/SMB
ファイル共有プロトコル（通信の仕組み）
- **Linux** → NFS でつなぐのが一般的
- **Windows** → SMB でつなぐのが一般的

### File Gateway の役割
S3データをNFSやSMB経由でオンプレから使えるようにするサービス

### サービス比較
- **DataSync** = データを「運ぶ」サービス
- **File Gateway** = データを「見せる／共有する」サービス

---

## Volume Gateway 詳細

### 機能
iSCSI経由でオンプレにブロックストレージを提供

### 2つのモード

#### Cached Volume
- マスターコピーはAWS（S3）
- オンプレには最近使うデータをキャッシュ
- クラウド中心で運用するイメージ

#### Stored Volume
- マスターコピーはオンプレ
- AWS（S3/EBSスナップショット）にはバックアップを保持
- オンプレ中心で運用するイメージ

---

## Tape Gateway

### 機能
**仮想テープライブラリ（VTL）**を提供

### 特徴
既存のバックアップソフトから見ればテープ装置に見えるが、実態はS3/Glacierに保存

---

## Gateway サービス用途比較

- **File Gateway** = データ共有用途（ファイルを複数人/複数システムで使用）
- **Volume Gateway** = 単一サーバーのディスク拡張やバックアップ用途（共有には向かない）

---

## 通知サービス比較

- **SNS** = ブロードキャスト型のイベント通知（同じ内容を複数先へ）
- **SES** = カスタム化されたメール送信（個別通知向け）

---

## VPN サービス

### AWS Client VPN
クライアント端末からAWS環境へ安全にアクセスできるマネージドVPNサービス

---

## AWS Direct Connect

### 基本概要
オンプレ ↔ AWS VPC を専用線でつなぐサービス

### 接続先
通常「VPCの Virtual Private Gateway (VGW)」または「Transit Gateway」

### Direct Connect Gateway (DXGW) の役割

#### 用途
複数のVPCやリージョンと接続したいときに使用するゲートウェイ

#### 制約と解決
- **VGW の制約**: 同じリージョンの1つのVPCにしか接続不可
- **DXGW の解決策**:
  - 複数のVPCに接続可能
  - リージョンをまたいで接続可能

---

## Amazon SageMaker サービス比較

### SageMaker Canvas
- ノーコードで使えるSageMakerのGUIツール
- 専門知識を持つ開発者が不足している場合などに使用可能

### SageMaker Studio
- ブラウザベースの統合ML開発環境（IDE）

### サービス比較表

| サービス | 対象ユーザー | 特徴 |
|----------|-------------|------|
| **SageMaker Canvas** | ビジネスユーザー / 分析担当者 | ノーコードで予測モデルを作れるGUI |
| **SageMaker Studio** | データサイエンティスト / MLエンジニア | コードベースの本格的なML開発IDE |

---

## File Gateway サービス比較

### File Gateway (S3用)
- S3 をオンプレからファイル共有として使用

### FSx File Gateway (FSx用)
- FSx for Windows File Server をオンプレから使用

### まとめ
両方とも「Storage Gatewayのファイル系サービス」だが、裏側のストレージが S3 か FSx かの違い

---

## AWS Transfer Family

### 概要
AWSがマネージドで提供するファイル転送サービス（旧AWS Transfer for SFTP）

### サポートプロトコル
SFTP / FTPS / FTP をサポート

### メリット
ユーザーはサーバー管理不要（EC2でSFTPサーバーを立てたり、OSパッチ管理をする必要なし）

---

## AWS Storage Gateway モード詳細

### File Gateway (S3 File Gateway)
- **機能**: S3 を NFS/SMB で見せる
- **保存先**: Amazon S3 バケット
- **ユースケース**: S3にバックアップ保存、ログ集約、アーカイブ

### FSx File Gateway
- **機能**: FSx (Windows File Server や Lustre) をオンプレに SMB で見せる
- **保存先**: Amazon FSx
- **ユースケース**: オンプレとAWS間で Windows ファイル共有を統合

---

## FSx for Lustre デプロイメントタイプ

- **Scratch** = 短期処理向け
- **Persistent** = 長期安定利用向け

---

## AWS Outposts

AWSサービスをオンプレミス環境に持ってこれる

---

## NAS（Network Attached Storage）

### 概要
ネットワーク（LAN）に接続して、複数のユーザーやサーバーから使えるファイル共有専用のストレージ機器

### プロトコル
ファイル共有プロトコル（NFS, SMB）を使用

---

## ファイル共有プロトコル比較

- **NFS** = UNIX/Linux 系でよく使われるファイル共有プロトコル
- **SMB** = Windows 系でよく使われるファイル共有プロトコル
- **HDFS** = Apache Hadoop 向けの分散ファイルシステム

---

## AWS Service Catalog

CloudFormationをラップして、承認済みのインフラを安全にセルフサービスで提供できるサービス

---

## AWS Marketplace

AWS上で動くソフトウェア・サービス・データ製品を購入・利用できる「公式のマーケットプレイス」

---

## DynamoDB Auto Scaling

### 基本概念
- プロビジョンドモードの場合に利用可能
- CloudWatch メトリクスを監視して、RCU/WCU を自動的に増減
- EC2 の ASG に相当する「需要に応じた自動スケーリング」

### 動作例
テーブルの使用率が 70% 超えたら RCU を増やす

### モード比較
- **プロビジョンド** = 最初に RCU/WCU を決めるけど、Auto Scalingで動的調整可能
- **オンデマンド** = 完全に自動、最初の設定すら不要

---

## RDS vs Aurora Auto Scaling

- **通常のRDS** → 自動リードレプリカ追加はできない（手動 or Lambdaスクリプト）
- **Aurora** → Aurora Auto Scaling で「CPUが上がったらリードレプリカ追加」が可能

---

## API Gateway スロットリング vs 使用量プラン

### スロットリングだけ
- 「このAPIは全体で1秒あたり100リクエストまで」
- 全員で共有 → 誰かが使いすぎたら他の人も影響を受ける

### 使用量プラン
- Vendor A → 1日10,000回まで、1秒あたり10リクエスト
- Vendor B → 1日5,000回まで、1秒あたり5リクエスト
- ベンダーごとに制御できる

### 使い分け例

#### 全体制御（スロットリング）
API全体で「1秒あたり500リクエストまで」

#### 顧客別制御（使用量プラン）
- 顧客A（大口）: 1日100,000リクエストまで、1秒あたり50リクエスト
- 顧客B（中小）: 1日10,000リクエストまで、1秒あたり5リクエスト
- 顧客C（無料プラン）: 1日1,000リクエストまで、1秒あたり2リクエスト

---

## Compute Savings Plans

### 概要
AWSのコスト最適化サービスの一つで、特に EC2 / Fargate / Lambda の料金を安くする仕組み

### Savings Plans比較

#### Compute Savings Plans
- 一番柔軟
- EC2、Fargate、Lambda 全部で使用可能
- インスタンスタイプ / OS / リージョンに縛られない

#### EC2 Instance Savings Plans
- 特定のインスタンスタイプ（例: m5.large、リージョン: ap-northeast-1）に縛られる
- その代わり Compute よりさらに割引率が高い

---

## Kinesis / Firehose 用語

Kinesis や Firehose の世界では「**Sink = 出力先**」を指す

---

## Direct Connect（専用線）

### 概要
オンプレミス環境から AWS への専用物理回線

### 特徴
S3 含め、EC2 や RDS など AWS のあらゆるサービスに安定した低レイテンシ接続を提供

---

## Storage Gateway ボリュームタイプ

- **キャッシュ型ボリューム** = S3がプライマリ
- **ストア型ボリューム** = オンプレがプライマリ

---

## Lambda IAM ロール vs リソースベースポリシー

- **実行ロール** → Lambda が「能動的にアクセス」するとき
- **リソースベースポリシー** → Lambda が「受動的に呼ばれる」とき（lambda:InvokeFunction）

---

## AWS ファイルシステムサービス比較

- **EFS** → 汎用NFS（小〜中規模、Linuxアプリ用）
- **FSx for Windows File Server** → SMBベース、EFSのWindows版っぽい立ち位置
- **FSx for Lustre** → HPC用途のLinuxファイルシステム（大規模・超高速I/O）
- **FSx for ONTAP / OpenZFS** → エンタープライズ向け、既存NAS資産のクラウド移行向け

---

## SCP（Service Control Policy）の使い方

- 「SCP でリソース作成を許可する」 → ❌（SCPは許可しない）
- 「SCP でタグがない場合は拒否する」 → ✅（正しい使い方）

---

## MQTT

IoT デバイスのために作られた軽量Pub/Subプロトコル